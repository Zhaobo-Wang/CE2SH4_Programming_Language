A string is an array of characters, ending with character '\0'

"Hello" is the array with elements: 'H''e''l''l''o''\0'

char c[] = { 'H','e','l','l','o' };
上面这个只是一个char array

char c[] = { 'H','e','l','l','o','\0' };
上面这个是一个char array 也是一个string 

char array 包含 string
——————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————
size of a string

size of a string 是number of character before symbol '\0'

________________________________________________________________________________________________________________________________________________________________________________________________________________________

defining and initializing string

两种方式：

1） char c[] = { 'H','e','l','l','o','\0' };

2） char c[] = "Hello";

* size of the string is 5.

*  size of the array is 6.

———————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————

looping through a string

# include <stdio.h>
int main(void){
  char b[] = "blue";
  int i;
  for (i = 0; b[i]!='\0'; i++){
  }
  return 0;
}

_____________________________________________________________________________________________________________________________________________________________________________________________________________________________

计算 string 的size大小

# include <stdio.h>
int main(void){
  char b[] = "blue";
  int i;
  int size = 0;
  for (i = 0; b[i]!='\0'; i++){
    size ++;
  }
  printf("The string has %d sizes \n", size);
  return 0;
}

_____________________________________________________________________________________________________________________________________________________________________________________________________________________________

passing a string to a function

可以在function的parameter只输入name of the string 就可以，不一定必须要包含size

举个例子：
void reverse_string( char x[] ){

}

_________________________________________________________________________________________________________________________________________________________________________________________________________________________________

Example reverse a input string:

#include <stdio.h>
#include <stdlib.h>

int main(){

    char x[20];

    puts("Please enter the string: ");

    gets(x);

    printf("The string is %s \n", x);

    reverse_string(x);

    printf("The string is %s", x);

}

void reverse_string( char x[]){
    int i, j, size = 0, temp;
    while(x[size] != '\0'){
        size++;
    }

    for (i = 0, j = size - 1; i < size/2; i++, j --){
        temp = x[i];
        x[i] = x[j];
        x[j] = temp;
    }
}

_____________________________________________________________________________________________________________________________________________________________________________________________________





